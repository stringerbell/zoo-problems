{
  "name": "zoo-problems",
  "version": "1.0.0",
  "description": "A stealth comedy game where you play as a giraffe causing mischief at the zoo",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "vite": "^6.2.0",
    "gh-pages": "^6.3.0"
  }
}
